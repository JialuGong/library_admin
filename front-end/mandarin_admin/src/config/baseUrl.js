const baseUrl = process.env.NODE_ENV === 'production' ? '/' : '/apis'
// const hostname = 'localhost'
// const port = '8000'
// const parentPath = 'mandarin'
// const baseUrl = hostname + ':' + port + '/' + parentPath
export default baseUrl
